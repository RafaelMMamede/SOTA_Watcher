# Legacy prompt examples

The text files here document the retired abstract-only read/skim/ignore workflow.
They are not loaded by the current runner. Full-text eligibility uses the single
versioned prompt/schema in `screening/ollama.py` and the criteria in your
`search_terms.yaml`. Edit criteria there, rather than adapting legacy prompts.
